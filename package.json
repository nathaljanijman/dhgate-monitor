{
  "name": "dhgate-monitor",
  "version": "3.0.0",
  "description": "DHgate Kids Monitor - Pure Cloudflare Workers Application",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "tail": "wrangler tail",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}